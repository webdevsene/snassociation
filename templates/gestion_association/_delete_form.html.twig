<form method="post" action="{{ path('gestion_association_delete', {'id': gestion_association.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gestion_association.id) }}">
    <button class="btn">Delete</button>
</form>
